
/*
 *
 * 5.1
 *
 *
 */

int main () {

	// A null statement is just a semicolon ;
	// For example, we can use it for a while loop (while loops require a statement) if the condition can do all the work in the loop.
	int i = 0;
	while (i++ != 10)
		;
	return 0;
}
