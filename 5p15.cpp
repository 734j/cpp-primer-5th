
/*
 *
 * 5.15
 *
 *
 */

int main () {

	/* Corrected versions
	  
	  (a)
	  int ix = 0;
	  for ( ; ix != sz ; ++ix) {}
	  if (ix != sz)
	  
	  (b)
	  int ix = 0;
	  for ( ; ix != sz ; ++ix) {}
	  
	  (c)
	  for (int ix = 0 ; ix != sz ; ++ix) {}
	 */
	return 0;
}
